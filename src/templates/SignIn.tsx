import React, {FC} from 'react';

interface Props {
}

const SignIn: FC<Props> = (props: Props) => {
    return (
        <div>
            Sign In Template
        </div>
    );
};

export default SignIn;
